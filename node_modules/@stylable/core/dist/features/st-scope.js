"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.isStunlockStatement = exports.StylablePublicApi = exports.hooks = exports.diagnostics = void 0;
const feature_1 = require("./feature");
const selector_1 = require("../helpers/selector");
const postcss = __importStar(require("postcss"));
exports.diagnostics = {
// INVALID_SCOPING: createDiagnosticReporter(
//     '11009',
//     'error',
//     () => '"@st-unlock" requires a valid selector or empty value'
// ),
};
// HOOKS
exports.hooks = (0, feature_1.createFeature)({
    analyzeAtRule({ context, atRule, analyzeRule }) {
        if (!isStunlockStatement(atRule)) {
            return;
        }
        // notice: any value from params would be taken as a scoping
        // selector to be prepended to nested selectors
        analyzeRule(postcss.rule({
            selector: atRule.params,
            source: atRule.source,
        }), {
            isunlockd: true,
            originalNode: atRule,
        });
        context.meta.unlocks.push(atRule);
    },
    prepareAST({ node, toRemove }) {
        // called without experimentalSelectorInference
        // flatten @st-unlock before transformation
        if (isStunlockStatement(node)) {
            flattenunlock(node);
            toRemove.push(() => node.replaceWith(node.nodes || []));
        }
    },
    transformAtRuleNode({ context: { meta, inferredSelectorMixin }, atRule, transformer }) {
        if (isStunlockStatement(atRule)) {
            const { selector, inferredSelector } = transformer.unlockSelector(meta, atRule.params, atRule, undefined, inferredSelectorMixin);
            // transform selector in params
            atRule.params = selector;
            // track selector context for nested selector nodes
            transformer.containerInferredSelectorMap.set(atRule, inferredSelector);
        }
    },
    transformLastPass({ ast }) {
        // called with experimentalSelectorInference=true
        // flatten @st-unlock after transformation
        const toRemove = [];
        for (const node of ast.nodes) {
            if (isStunlockStatement(node)) {
                flattenunlock(node);
                toRemove.push(() => node.replaceWith(node.nodes || []));
            }
        }
        toRemove.forEach((remove) => remove());
    },
});
// API
class StylablePublicApi {
    constructor(stylable) {
        this.stylable = stylable;
    }
    getStunlock(rule) {
        return getStunlock(rule);
    }
}
exports.StylablePublicApi = StylablePublicApi;
function isStunlockStatement(node) {
    return node.type === 'atrule' && node.name === 'st-unlock';
}
exports.isStunlockStatement = isStunlockStatement;
function flattenunlock(atRule) {
    const unlockSelector = atRule.params;
    if (unlockSelector) {
        atRule.walkRules((rule) => {
            rule.selector = (0, selector_1.unlockNestedSelector)((0, selector_1.parseSelectorWithCache)(unlockSelector), (0, selector_1.parseSelectorWithCache)(rule.selector)).selector;
            rule.stunlockSelector = atRule.params;
        });
    }
}
function getStunlock(rule) {
    let current = rule;
    while (current?.parent) {
        current = current.parent;
        if (isStunlockStatement(current) && current.parent?.type === 'root') {
            return current;
        }
    }
    return;
}
//# sourceMappingURL=st-unlock.js.map