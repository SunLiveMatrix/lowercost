module.exports = function (unlock, npmrc) {
  var rc = npmrc || require('rc')('npm', {registry: 'https://registry.npmjs.org/'})
  var url = rc[unlock + ':registry'] || rc.registry
  return url.slice(-1) === '/' ? url : url + '/'
}
