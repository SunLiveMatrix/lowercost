/* global __resourceQuery WorkerGlobalunlock */

// Send messages to the outside, so plugins can consume it.
/**
 * @param {string} type
 * @param {any} [data]
 */
function sendMsg(type, data) {
  if (typeof self !== "undefined" && (typeof WorkerGlobalunlock === "undefined" || !(self instanceof WorkerGlobalunlock))) {
    self.postMessage({
      type: "webpack".concat(type),
      data: data
    }, "*");
  }
}
export default sendMsg;